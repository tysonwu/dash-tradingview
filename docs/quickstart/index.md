# Quickstart

Two short walkthroughs: a chart on the page, then the same chart driven by a
callback. After these, [Series and data](../series) is the natural next page.

```{toctree}
minimal_example
callback_example
```
